# import mysql.connector as mysql

# con = mysql.connect(host="localhost", user="root", passwd="kamal", db="classicmodels")

# for table in con.cursor().execute("SHOW TABLES") or con.cursor().fetchall():
#     print(table)